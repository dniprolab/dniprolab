#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../customizr/bin/customizr" "$@"
  ret=$?
else 
  node  "$basedir/../customizr/bin/customizr" "$@"
  ret=$?
fi
exit $ret
